# Development Dockerfile — runs Vite dev server with HMR

FROM node:22-alpine

WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

# Source files are bind-mounted via docker-compose.dev.yml
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
